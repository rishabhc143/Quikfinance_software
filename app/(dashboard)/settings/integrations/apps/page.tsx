import { ComingSoon } from "@/components/shared/settings-shell";
export const metadata = { title: "Integrations — Apps" };
export default function Page() {
  return <ComingSoon title="Integrations — Apps" description="Connect Quikfinance with external services." />;
}
