# Documents module

**Status: list/create complete; direct upload deferred.**

`Document` rows store metadata + URL. The "Add document" form expects an externally-hosted URL (Drive, Dropbox, S3 link, etc.); native upload to S3/UploadThing arrives once the upload provider is wired in.
